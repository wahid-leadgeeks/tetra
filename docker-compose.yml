services:
  db:
    image: postgres:16-alpine
    container_name: tetra-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tetra}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tetra_dev}
      POSTGRES_DB: ${POSTGRES_DB:-tetra}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - tetra-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tetra} -d ${POSTGRES_DB:-tetra}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  tetra-db-data:
